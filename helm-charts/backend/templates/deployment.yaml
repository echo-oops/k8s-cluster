{{- /*
Deployment for backend service.
- Uses image.repository and image.tag
- Mounts ConfigMap and optional Secret (SealedSecret-created Secret)
- Exposes application port and metrics port
- Readiness and liveness probes configurable via values
- SecurityContext and PodSecurityContext set from values
*/ -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "backend.fullname" . }}
  labels:
    {{ include "backend.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount | default 2 }}
  selector:
    matchLabels:
      app: {{ include "backend.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "backend.fullname" . }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        checksum/config: {{ include "backend.fullname" . }}-{{ sha256sum (toYaml .Values.config) }}
    spec:
      serviceAccountName: {{ default "backend-sa" .Values.serviceAccount.name | quote }}
      securityContext:
        {{- if .Values.podSecurityContext }}
        runAsUser: {{ .Values.podSecurityContext.runAsUser | default 1001 }}
        runAsGroup: {{ .Values.podSecurityContext.runAsGroup | default 2001 }}
        fsGroup: {{ .Values.podSecurityContext.fsGroup | default 2001 }}
        {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort | default 8080 }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.service.metricsPort | default 9100 }}
              protocol: TCP
          env:
            # Standard downward API
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          envFrom:
            - configMapRef:
                name: {{ include "backend.fullname" . }}-config
          {{- if and .Values.secrets.useSealedSecrets .Values.secrets.sealedSecretName }}
            - secretRef:
                name: {{ .Values.secrets.sealedSecretName }}
          {{- end }}
          {{- if .Values.env }}
          {{- range $i, $e := .Values.env }}
          - name: {{ $e.name }}
            value: {{ $e.value | quote }}
          {{- end }}
          {{- end }}
          readinessProbe:
            {{- if .Values.readinessProbe.httpGet }}
            httpGet:
              path: {{ .Values.readinessProbe.httpGet.path | default "/health" }}
              port: {{ .Values.readinessProbe.httpGet.port | default 8080 }}
            {{- end }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds | default 5 }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds | default 10 }}
            timeoutSeconds: {{ .Values.readinessProbe.timeoutSeconds | default 2 }}
            failureThreshold: {{ .Values.readinessProbe.failureThreshold | default 3 }}
            successThreshold: {{ .Values.readinessProbe.successThreshold | default 1 }}
          livenessProbe:
            {{- if .Values.livenessProbe.httpGet }}
            httpGet:
              path: {{ .Values.livenessProbe.httpGet.path | default "/live" }}
              port: {{ .Values.livenessProbe.httpGet.port | default 8080 }}
            {{- end }}
            initialDelaySeconds: {{ .Values.livenessProbe.initialDelaySeconds | default 15 }}
            periodSeconds: {{ .Values.livenessProbe.periodSeconds | default 20 }}
            timeoutSeconds: {{ .Values.livenessProbe.timeoutSeconds | default 3 }}
            failureThreshold: {{ .Values.livenessProbe.failureThreshold | default 5 }}
            successThreshold: {{ .Values.livenessProbe.successThreshold | default 1 }}
          resources:
            requests:
              cpu: {{ .Values.resources.requests.cpu | default "150m" }}
              memory: {{ .Values.resources.requests.memory | default "192Mi" }}
            limits:
              cpu: {{ .Values.resources.limits.cpu | default "300m" }}
              memory: {{ .Values.resources.limits.memory | default "384Mi" }}
          securityContext:
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation | default false }}
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem | default true }}
            capabilities:
              drop:
                - ALL
      {{- if .Values.affinity }}
      affinity:
        {{ toYaml .Values.affinity | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations:
        {{ toYaml .Values.tolerations | nindent 8 }}
      {{- end }}
