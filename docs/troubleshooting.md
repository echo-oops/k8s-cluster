# Troubleshooting

Этот документ содержит практические шаги и команды для диагностики и устранения распространённых проблем при развертывании и эксплуатации кластера.

## Общие проверки
- Проверить состояние нод:
  ```bash
  kubectl get nodes -o wide
  kubectl get pods -A
